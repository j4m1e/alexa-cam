{
  "name": "alexa-cam",
  "version": "0.0.1",
  "description": "Lambda function that calls CAM API to create an AWS instance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jamie Tyler",
  "license": "ISC",
  "dependencies": {
    "alexa-sdk": "^1.0.11"
  }
}
